spring:
  application:
    name: user-server
  cloud:
    config:
      uri: http://config-server:31000
      #profile: prod
      name: common-config,user-server

app:
  id: ${spring.application.name}